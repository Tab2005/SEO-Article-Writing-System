# Frontend Environment Variables
# Copy to .env.local and fill in values

# Google OAuth Client ID (from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=

# Backend API URL (leave empty for proxy, or set to full backend URL)
VITE_API_URL=
